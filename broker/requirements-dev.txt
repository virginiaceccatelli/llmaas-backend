# Tools for working on the broker locally. Not installed into the container.
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
ruff==0.9.2          # linter + formatter:  ruff check .   /   ruff format .
