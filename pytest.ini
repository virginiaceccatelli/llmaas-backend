[pytest]
# `app` lives under broker/, so tests can `from app.security import ...`
pythonpath = broker
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
